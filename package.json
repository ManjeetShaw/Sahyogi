{
  "name": "sahyogi",
  "version": "1.0.0",
  "private": true,
  "description": "GenAI-powered civic platform: government services access, public issue reporting, and an AI companion (Sahyogi).",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "license": "MIT"
}
